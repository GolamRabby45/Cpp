#include<string>
#include<cstring>
#include<iostream>

/*
    Base b;
    b.deposit();  -- will call Base::deposit

    Derived d;
    d.deposit(); -- will call Derived::deposit

    Base *ptr = new Derived();
    Base *ptr {nullptr};
    ptr = new Derived();
    ptr = new Base();

    ptr->deposit(1000); -- which to call Base::dposit() || Derived::deposit() 

*/


int main(){
    return 0;
}